akka {
    loglevel = INFO
}

spray.can.server {
  request-timeout = 1s
}

spray {
    can {
        server {
            request-timeout = 1s
        }
    }
}

storage {
    state = "data/state.conf"
}

include "secrets"
